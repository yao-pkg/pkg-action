{
  "name": "@pkg-action/build",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Node 24 JS action behind the top-level pkg-action entrypoint. Orchestrates cache → install pkg → build → (optional windows-metadata + sign) → archive → checksum.",
  "main": "./src/main.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pkg-action/core": "*",
    "@actions/cache": "^6.2.0",
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/glob": "^0.6.1"
  }
}
