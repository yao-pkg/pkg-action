name: 'pkg-action (M0 scaffold — not yet functional)'
description: 'Build, sign, archive, and publish Node.js binaries with @yao-pkg/pkg. NOT YET RELEASED — do not consume until v1.0.0.'
author: 'yao-pkg contributors'
branding:
  icon: 'package'
  color: 'blue'

# NOTE: This top-level action.yml is a STUB. In v1 it will be code-generated by
# scripts/gen-action-yml.ts from packages/core/src/inputs.ts metadata, with the
# full ~40-input surface defined in the plan (yao-pkg/pkg#248).

runs:
  using: 'composite'
  steps:
    - name: Warn — pkg-action is not yet released
      shell: bash
      run: |
        echo "::warning::yao-pkg/pkg-action is in M0 scaffold state and not functional yet."
        echo "::warning::Track progress at https://github.com/yao-pkg/pkg/issues/248"
        exit 1
